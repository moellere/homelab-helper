"""Database layer: SQLAlchemy 2.0 declarative models, enums, session factory.

Models live in :mod:`homelab_helper.db.models` (per-concept files).
The declarative ``Base`` and shared utilities (UUIDv7, UTC ``now``) live in
:mod:`homelab_helper.db.base`.
"""
